services:
  - type: web
    name: trans-mcq-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run build && node dist/server.ts
    envVars:
      - key: MONGODB_URI
        scope: build
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
