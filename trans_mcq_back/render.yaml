services:
  - type: web
    name: trans-mcq-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: MONGODB_URI
        scope: build
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
